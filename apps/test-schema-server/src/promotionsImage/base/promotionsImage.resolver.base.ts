/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { PromotionsImage } from "./PromotionsImage";
import { PromotionsImageCountArgs } from "./PromotionsImageCountArgs";
import { PromotionsImageFindManyArgs } from "./PromotionsImageFindManyArgs";
import { PromotionsImageFindUniqueArgs } from "./PromotionsImageFindUniqueArgs";
import { CreatePromotionsImageArgs } from "./CreatePromotionsImageArgs";
import { UpdatePromotionsImageArgs } from "./UpdatePromotionsImageArgs";
import { DeletePromotionsImageArgs } from "./DeletePromotionsImageArgs";
import { PromotionFindManyArgs } from "../../promotion/base/PromotionFindManyArgs";
import { Promotion } from "../../promotion/base/Promotion";
import { PromotionsImageService } from "../promotionsImage.service";
@graphql.Resolver(() => PromotionsImage)
export class PromotionsImageResolverBase {
  constructor(protected readonly service: PromotionsImageService) {}

  async _promotionsImagesMeta(
    @graphql.Args() args: PromotionsImageCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [PromotionsImage])
  async promotionsImages(
    @graphql.Args() args: PromotionsImageFindManyArgs
  ): Promise<PromotionsImage[]> {
    return this.service.promotionsImages(args);
  }

  @graphql.Query(() => PromotionsImage, { nullable: true })
  async promotionsImage(
    @graphql.Args() args: PromotionsImageFindUniqueArgs
  ): Promise<PromotionsImage | null> {
    const result = await this.service.promotionsImage(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => PromotionsImage)
  async createPromotionsImage(
    @graphql.Args() args: CreatePromotionsImageArgs
  ): Promise<PromotionsImage> {
    return await this.service.createPromotionsImage({
      ...args,
      data: args.data,
    });
  }

  @graphql.Mutation(() => PromotionsImage)
  async updatePromotionsImage(
    @graphql.Args() args: UpdatePromotionsImageArgs
  ): Promise<PromotionsImage | null> {
    try {
      return await this.service.updatePromotionsImage({
        ...args,
        data: args.data,
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => PromotionsImage)
  async deletePromotionsImage(
    @graphql.Args() args: DeletePromotionsImageArgs
  ): Promise<PromotionsImage | null> {
    try {
      return await this.service.deletePromotionsImage(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.ResolveField(() => [Promotion], { name: "promotions" })
  async findPromotions(
    @graphql.Parent() parent: PromotionsImage,
    @graphql.Args() args: PromotionFindManyArgs
  ): Promise<Promotion[]> {
    const results = await this.service.findPromotions(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }
}
