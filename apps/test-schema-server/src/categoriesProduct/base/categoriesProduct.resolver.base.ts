/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { CategoriesProduct } from "./CategoriesProduct";
import { CategoriesProductCountArgs } from "./CategoriesProductCountArgs";
import { CategoriesProductFindManyArgs } from "./CategoriesProductFindManyArgs";
import { CategoriesProductFindUniqueArgs } from "./CategoriesProductFindUniqueArgs";
import { CreateCategoriesProductArgs } from "./CreateCategoriesProductArgs";
import { UpdateCategoriesProductArgs } from "./UpdateCategoriesProductArgs";
import { DeleteCategoriesProductArgs } from "./DeleteCategoriesProductArgs";
import { Category } from "../../category/base/Category";
import { CategoriesProductService } from "../categoriesProduct.service";
@graphql.Resolver(() => CategoriesProduct)
export class CategoriesProductResolverBase {
  constructor(protected readonly service: CategoriesProductService) {}

  async _categoriesProductsMeta(
    @graphql.Args() args: CategoriesProductCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [CategoriesProduct])
  async categoriesProducts(
    @graphql.Args() args: CategoriesProductFindManyArgs
  ): Promise<CategoriesProduct[]> {
    return this.service.categoriesProducts(args);
  }

  @graphql.Query(() => CategoriesProduct, { nullable: true })
  async categoriesProduct(
    @graphql.Args() args: CategoriesProductFindUniqueArgs
  ): Promise<CategoriesProduct | null> {
    const result = await this.service.categoriesProduct(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => CategoriesProduct)
  async createCategoriesProduct(
    @graphql.Args() args: CreateCategoriesProductArgs
  ): Promise<CategoriesProduct> {
    return await this.service.createCategoriesProduct({
      ...args,
      data: {
        ...args.data,

        category: {
          connect: args.data.category,
        },
      },
    });
  }

  @graphql.Mutation(() => CategoriesProduct)
  async updateCategoriesProduct(
    @graphql.Args() args: UpdateCategoriesProductArgs
  ): Promise<CategoriesProduct | null> {
    try {
      return await this.service.updateCategoriesProduct({
        ...args,
        data: {
          ...args.data,

          category: {
            connect: args.data.category,
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => CategoriesProduct)
  async deleteCategoriesProduct(
    @graphql.Args() args: DeleteCategoriesProductArgs
  ): Promise<CategoriesProduct | null> {
    try {
      return await this.service.deleteCategoriesProduct(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.ResolveField(() => Category, {
    nullable: true,
    name: "category",
  })
  async getCategory(
    @graphql.Parent() parent: CategoriesProduct
  ): Promise<Category | null> {
    const result = await this.service.getCategory(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
