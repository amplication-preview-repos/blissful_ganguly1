/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { FundraisersPromotion } from "./FundraisersPromotion";
import { FundraisersPromotionCountArgs } from "./FundraisersPromotionCountArgs";
import { FundraisersPromotionFindManyArgs } from "./FundraisersPromotionFindManyArgs";
import { FundraisersPromotionFindUniqueArgs } from "./FundraisersPromotionFindUniqueArgs";
import { CreateFundraisersPromotionArgs } from "./CreateFundraisersPromotionArgs";
import { UpdateFundraisersPromotionArgs } from "./UpdateFundraisersPromotionArgs";
import { DeleteFundraisersPromotionArgs } from "./DeleteFundraisersPromotionArgs";
import { Fundraiser } from "../../fundraiser/base/Fundraiser";
import { Promotion } from "../../promotion/base/Promotion";
import { FundraisersPromotionService } from "../fundraisersPromotion.service";
@graphql.Resolver(() => FundraisersPromotion)
export class FundraisersPromotionResolverBase {
  constructor(protected readonly service: FundraisersPromotionService) {}

  async _fundraisersPromotionsMeta(
    @graphql.Args() args: FundraisersPromotionCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [FundraisersPromotion])
  async fundraisersPromotions(
    @graphql.Args() args: FundraisersPromotionFindManyArgs
  ): Promise<FundraisersPromotion[]> {
    return this.service.fundraisersPromotions(args);
  }

  @graphql.Query(() => FundraisersPromotion, { nullable: true })
  async fundraisersPromotion(
    @graphql.Args() args: FundraisersPromotionFindUniqueArgs
  ): Promise<FundraisersPromotion | null> {
    const result = await this.service.fundraisersPromotion(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => FundraisersPromotion)
  async createFundraisersPromotion(
    @graphql.Args() args: CreateFundraisersPromotionArgs
  ): Promise<FundraisersPromotion> {
    return await this.service.createFundraisersPromotion({
      ...args,
      data: {
        ...args.data,

        fundraisers: {
          connect: args.data.fundraisers,
        },

        promotions: {
          connect: args.data.promotions,
        },
      },
    });
  }

  @graphql.Mutation(() => FundraisersPromotion)
  async updateFundraisersPromotion(
    @graphql.Args() args: UpdateFundraisersPromotionArgs
  ): Promise<FundraisersPromotion | null> {
    try {
      return await this.service.updateFundraisersPromotion({
        ...args,
        data: {
          ...args.data,

          fundraisers: {
            connect: args.data.fundraisers,
          },

          promotions: {
            connect: args.data.promotions,
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => FundraisersPromotion)
  async deleteFundraisersPromotion(
    @graphql.Args() args: DeleteFundraisersPromotionArgs
  ): Promise<FundraisersPromotion | null> {
    try {
      return await this.service.deleteFundraisersPromotion(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.ResolveField(() => Fundraiser, {
    nullable: true,
    name: "fundraisers",
  })
  async getFundraisers(
    @graphql.Parent() parent: FundraisersPromotion
  ): Promise<Fundraiser | null> {
    const result = await this.service.getFundraisers(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @graphql.ResolveField(() => Promotion, {
    nullable: true,
    name: "promotions",
  })
  async getPromotions(
    @graphql.Parent() parent: FundraisersPromotion
  ): Promise<Promotion | null> {
    const result = await this.service.getPromotions(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
