/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { Fundraiser } from "./Fundraiser";
import { FundraiserCountArgs } from "./FundraiserCountArgs";
import { FundraiserFindManyArgs } from "./FundraiserFindManyArgs";
import { FundraiserFindUniqueArgs } from "./FundraiserFindUniqueArgs";
import { CreateFundraiserArgs } from "./CreateFundraiserArgs";
import { UpdateFundraiserArgs } from "./UpdateFundraiserArgs";
import { DeleteFundraiserArgs } from "./DeleteFundraiserArgs";
import { FundraisersPromotionFindManyArgs } from "../../fundraisersPromotion/base/FundraisersPromotionFindManyArgs";
import { FundraisersPromotion } from "../../fundraisersPromotion/base/FundraisersPromotion";
import { FundraisersCustomerFindManyArgs } from "../../fundraisersCustomer/base/FundraisersCustomerFindManyArgs";
import { FundraisersCustomer } from "../../fundraisersCustomer/base/FundraisersCustomer";
import { OrderFindManyArgs } from "../../order/base/OrderFindManyArgs";
import { Order } from "../../order/base/Order";
import { FundraisersProductFindManyArgs } from "../../fundraisersProduct/base/FundraisersProductFindManyArgs";
import { FundraisersProduct } from "../../fundraisersProduct/base/FundraisersProduct";
import { FundraisersImage } from "../../fundraisersImage/base/FundraisersImage";
import { FundraisersStatus } from "../../fundraisersStatus/base/FundraisersStatus";
import { Topic } from "../../topic/base/Topic";
import { UserRole } from "../../userRole/base/UserRole";
import { FundraiserService } from "../fundraiser.service";
@graphql.Resolver(() => Fundraiser)
export class FundraiserResolverBase {
  constructor(protected readonly service: FundraiserService) {}

  async _fundraisersMeta(
    @graphql.Args() args: FundraiserCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [Fundraiser])
  async fundraisers(
    @graphql.Args() args: FundraiserFindManyArgs
  ): Promise<Fundraiser[]> {
    return this.service.fundraisers(args);
  }

  @graphql.Query(() => Fundraiser, { nullable: true })
  async fundraiser(
    @graphql.Args() args: FundraiserFindUniqueArgs
  ): Promise<Fundraiser | null> {
    const result = await this.service.fundraiser(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => Fundraiser)
  async createFundraiser(
    @graphql.Args() args: CreateFundraiserArgs
  ): Promise<Fundraiser> {
    return await this.service.createFundraiser({
      ...args,
      data: {
        ...args.data,

        fundraisersimages: args.data.fundraisersimages
          ? {
              connect: args.data.fundraisersimages,
            }
          : undefined,

        fundraisersstatus: args.data.fundraisersstatus
          ? {
              connect: args.data.fundraisersstatus,
            }
          : undefined,

        topics: args.data.topics
          ? {
              connect: args.data.topics,
            }
          : undefined,

        userRoles: args.data.userRoles
          ? {
              connect: args.data.userRoles,
            }
          : undefined,

        userRolesFundraisersSupplierUuidToUserRoles: args.data
          .userRolesFundraisersSupplierUuidToUserRoles
          ? {
              connect: args.data.userRolesFundraisersSupplierUuidToUserRoles,
            }
          : undefined,
      },
    });
  }

  @graphql.Mutation(() => Fundraiser)
  async updateFundraiser(
    @graphql.Args() args: UpdateFundraiserArgs
  ): Promise<Fundraiser | null> {
    try {
      return await this.service.updateFundraiser({
        ...args,
        data: {
          ...args.data,

          fundraisersimages: args.data.fundraisersimages
            ? {
                connect: args.data.fundraisersimages,
              }
            : undefined,

          fundraisersstatus: args.data.fundraisersstatus
            ? {
                connect: args.data.fundraisersstatus,
              }
            : undefined,

          topics: args.data.topics
            ? {
                connect: args.data.topics,
              }
            : undefined,

          userRoles: args.data.userRoles
            ? {
                connect: args.data.userRoles,
              }
            : undefined,

          userRolesFundraisersSupplierUuidToUserRoles: args.data
            .userRolesFundraisersSupplierUuidToUserRoles
            ? {
                connect: args.data.userRolesFundraisersSupplierUuidToUserRoles,
              }
            : undefined,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => Fundraiser)
  async deleteFundraiser(
    @graphql.Args() args: DeleteFundraiserArgs
  ): Promise<Fundraiser | null> {
    try {
      return await this.service.deleteFundraiser(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.ResolveField(() => [FundraisersPromotion], {
    name: "fundraisersPromotions",
  })
  async findFundraisersPromotions(
    @graphql.Parent() parent: Fundraiser,
    @graphql.Args() args: FundraisersPromotionFindManyArgs
  ): Promise<FundraisersPromotion[]> {
    const results = await this.service.findFundraisersPromotions(
      parent.id,
      args
    );

    if (!results) {
      return [];
    }

    return results;
  }

  @graphql.ResolveField(() => [FundraisersCustomer], {
    name: "fundraiserscustomers",
  })
  async findFundraiserscustomers(
    @graphql.Parent() parent: Fundraiser,
    @graphql.Args() args: FundraisersCustomerFindManyArgs
  ): Promise<FundraisersCustomer[]> {
    const results = await this.service.findFundraiserscustomers(
      parent.id,
      args
    );

    if (!results) {
      return [];
    }

    return results;
  }

  @graphql.ResolveField(() => [Order], { name: "fundraisersorders" })
  async findFundraisersorders(
    @graphql.Parent() parent: Fundraiser,
    @graphql.Args() args: OrderFindManyArgs
  ): Promise<Order[]> {
    const results = await this.service.findFundraisersorders(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @graphql.ResolveField(() => [FundraisersProduct], {
    name: "fundraisersproducts",
  })
  async findFundraisersproducts(
    @graphql.Parent() parent: Fundraiser,
    @graphql.Args() args: FundraisersProductFindManyArgs
  ): Promise<FundraisersProduct[]> {
    const results = await this.service.findFundraisersproducts(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @graphql.ResolveField(() => FundraisersImage, {
    nullable: true,
    name: "fundraisersimages",
  })
  async getFundraisersimages(
    @graphql.Parent() parent: Fundraiser
  ): Promise<FundraisersImage | null> {
    const result = await this.service.getFundraisersimages(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @graphql.ResolveField(() => FundraisersStatus, {
    nullable: true,
    name: "fundraisersstatus",
  })
  async getFundraisersstatus(
    @graphql.Parent() parent: Fundraiser
  ): Promise<FundraisersStatus | null> {
    const result = await this.service.getFundraisersstatus(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @graphql.ResolveField(() => Topic, {
    nullable: true,
    name: "topics",
  })
  async getTopics(@graphql.Parent() parent: Fundraiser): Promise<Topic | null> {
    const result = await this.service.getTopics(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @graphql.ResolveField(() => UserRole, {
    nullable: true,
    name: "userRoles",
  })
  async getUserRoles(
    @graphql.Parent() parent: Fundraiser
  ): Promise<UserRole | null> {
    const result = await this.service.getUserRoles(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @graphql.ResolveField(() => UserRole, {
    nullable: true,
    name: "userRolesFundraisersSupplierUuidToUserRoles",
  })
  async getUserRolesFundraisersSupplierUuidToUserRoles(
    @graphql.Parent() parent: Fundraiser
  ): Promise<UserRole | null> {
    const result =
      await this.service.getUserRolesFundraisersSupplierUuidToUserRoles(
        parent.id
      );

    if (!result) {
      return null;
    }
    return result;
  }
}
