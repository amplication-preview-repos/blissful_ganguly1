/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { FundraisersProduct } from "./FundraisersProduct";
import { FundraisersProductCountArgs } from "./FundraisersProductCountArgs";
import { FundraisersProductFindManyArgs } from "./FundraisersProductFindManyArgs";
import { FundraisersProductFindUniqueArgs } from "./FundraisersProductFindUniqueArgs";
import { CreateFundraisersProductArgs } from "./CreateFundraisersProductArgs";
import { UpdateFundraisersProductArgs } from "./UpdateFundraisersProductArgs";
import { DeleteFundraisersProductArgs } from "./DeleteFundraisersProductArgs";
import { Fundraiser } from "../../fundraiser/base/Fundraiser";
import { Product } from "../../product/base/Product";
import { FundraisersProductService } from "../fundraisersProduct.service";
@graphql.Resolver(() => FundraisersProduct)
export class FundraisersProductResolverBase {
  constructor(protected readonly service: FundraisersProductService) {}

  async _fundraisersProductsMeta(
    @graphql.Args() args: FundraisersProductCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [FundraisersProduct])
  async fundraisersProducts(
    @graphql.Args() args: FundraisersProductFindManyArgs
  ): Promise<FundraisersProduct[]> {
    return this.service.fundraisersProducts(args);
  }

  @graphql.Query(() => FundraisersProduct, { nullable: true })
  async fundraisersProduct(
    @graphql.Args() args: FundraisersProductFindUniqueArgs
  ): Promise<FundraisersProduct | null> {
    const result = await this.service.fundraisersProduct(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => FundraisersProduct)
  async createFundraisersProduct(
    @graphql.Args() args: CreateFundraisersProductArgs
  ): Promise<FundraisersProduct> {
    return await this.service.createFundraisersProduct({
      ...args,
      data: {
        ...args.data,

        fundraisers: {
          connect: args.data.fundraisers,
        },

        product: args.data.product
          ? {
              connect: args.data.product,
            }
          : undefined,
      },
    });
  }

  @graphql.Mutation(() => FundraisersProduct)
  async updateFundraisersProduct(
    @graphql.Args() args: UpdateFundraisersProductArgs
  ): Promise<FundraisersProduct | null> {
    try {
      return await this.service.updateFundraisersProduct({
        ...args,
        data: {
          ...args.data,

          fundraisers: {
            connect: args.data.fundraisers,
          },

          product: args.data.product
            ? {
                connect: args.data.product,
              }
            : undefined,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => FundraisersProduct)
  async deleteFundraisersProduct(
    @graphql.Args() args: DeleteFundraisersProductArgs
  ): Promise<FundraisersProduct | null> {
    try {
      return await this.service.deleteFundraisersProduct(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.ResolveField(() => Fundraiser, {
    nullable: true,
    name: "fundraisers",
  })
  async getFundraisers(
    @graphql.Parent() parent: FundraisersProduct
  ): Promise<Fundraiser | null> {
    const result = await this.service.getFundraisers(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @graphql.ResolveField(() => Product, {
    nullable: true,
    name: "product",
  })
  async getProduct(
    @graphql.Parent() parent: FundraisersProduct
  ): Promise<Product | null> {
    const result = await this.service.getProduct(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
