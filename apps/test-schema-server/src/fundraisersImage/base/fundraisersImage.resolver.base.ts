/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { FundraisersImage } from "./FundraisersImage";
import { FundraisersImageCountArgs } from "./FundraisersImageCountArgs";
import { FundraisersImageFindManyArgs } from "./FundraisersImageFindManyArgs";
import { FundraisersImageFindUniqueArgs } from "./FundraisersImageFindUniqueArgs";
import { CreateFundraisersImageArgs } from "./CreateFundraisersImageArgs";
import { UpdateFundraisersImageArgs } from "./UpdateFundraisersImageArgs";
import { DeleteFundraisersImageArgs } from "./DeleteFundraisersImageArgs";
import { FundraiserFindManyArgs } from "../../fundraiser/base/FundraiserFindManyArgs";
import { Fundraiser } from "../../fundraiser/base/Fundraiser";
import { FundraisersImageService } from "../fundraisersImage.service";
@graphql.Resolver(() => FundraisersImage)
export class FundraisersImageResolverBase {
  constructor(protected readonly service: FundraisersImageService) {}

  async _fundraisersImagesMeta(
    @graphql.Args() args: FundraisersImageCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [FundraisersImage])
  async fundraisersImages(
    @graphql.Args() args: FundraisersImageFindManyArgs
  ): Promise<FundraisersImage[]> {
    return this.service.fundraisersImages(args);
  }

  @graphql.Query(() => FundraisersImage, { nullable: true })
  async fundraisersImage(
    @graphql.Args() args: FundraisersImageFindUniqueArgs
  ): Promise<FundraisersImage | null> {
    const result = await this.service.fundraisersImage(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => FundraisersImage)
  async createFundraisersImage(
    @graphql.Args() args: CreateFundraisersImageArgs
  ): Promise<FundraisersImage> {
    return await this.service.createFundraisersImage({
      ...args,
      data: args.data,
    });
  }

  @graphql.Mutation(() => FundraisersImage)
  async updateFundraisersImage(
    @graphql.Args() args: UpdateFundraisersImageArgs
  ): Promise<FundraisersImage | null> {
    try {
      return await this.service.updateFundraisersImage({
        ...args,
        data: args.data,
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => FundraisersImage)
  async deleteFundraisersImage(
    @graphql.Args() args: DeleteFundraisersImageArgs
  ): Promise<FundraisersImage | null> {
    try {
      return await this.service.deleteFundraisersImage(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.ResolveField(() => [Fundraiser], { name: "fundraisers" })
  async findFundraisers(
    @graphql.Parent() parent: FundraisersImage,
    @graphql.Args() args: FundraiserFindManyArgs
  ): Promise<Fundraiser[]> {
    const results = await this.service.findFundraisers(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }
}
